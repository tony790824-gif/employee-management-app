[build]
  command = "pnpm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22.14.0"
  PNPM_VERSION = "11.9.0"

[functions]
  # Static frontend only. Archived adapters are not deployed and push stays deferred.
  directory = "netlify/static-only"

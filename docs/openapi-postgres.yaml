openapi: 3.0.3
info:
  title: 班客邦 PostgreSQL Command API
  version: 2026-07-27.2
  description: Implemented Staging transition API with accepted Auth0 Staging PKCE/refresh, OIDC/JWKS validation, local Session boundaries, and the Phase 1 time-off request data/command surface. The time-off frontend UI and Production cutover are not approved.
  x-baseline-commit: a3da8c39e0f7b012a24c47fd21073b8b4da1bec3
  x-project-completion: 87
  x-staging-migration: 0013_time_off_requests
servers:
  - url: http://127.0.0.1:8080/v1
    description: Local development only
security:
  - BearerAuth: []
paths:
  /health:
    get:
      security: []
      responses:
        '200': { description: Process is running }
  /readiness:
    get:
      security: []
      responses:
        '200': { description: Database is reachable }
        '500': { $ref: '#/components/responses/Error' }
  /auth/session:
    post:
      parameters:
        - $ref: '#/components/parameters/WorkspaceId'
      responses:
        '201': { description: Local revocable session established after live membership authorization }
        '401': { $ref: '#/components/responses/Error' }
        '403': { $ref: '#/components/responses/Error' }
  /auth/logout:
    post:
      parameters:
        - $ref: '#/components/parameters/WorkspaceId'
      responses:
        '200': { description: Local session revoked }
        '401': { $ref: '#/components/responses/Error' }
        '403': { $ref: '#/components/responses/Error' }
  /employees:
    get:
      parameters:
        - $ref: '#/components/parameters/WorkspaceId'
      responses:
        '200': { description: Active employees visible to boss or manager }
        '401': { $ref: '#/components/responses/Error' }
        '403': { $ref: '#/components/responses/Error' }
  /time-off-requests:
    get:
      summary: Read role-scoped scheduled-leave and ad-hoc-leave request data
      parameters:
        - $ref: '#/components/parameters/WorkspaceId'
      responses:
        '200':
          description: Own requests, manager review data, and privacy-filtered same-store approved coverage
        '401': { $ref: '#/components/responses/Error' }
        '403': { $ref: '#/components/responses/Error' }
  /commands/{commandName}:
    post:
      parameters:
        - $ref: '#/components/parameters/WorkspaceId'
        - in: path
          name: commandName
          required: true
          schema:
            type: string
            enum:
              - employees.create
              - shifts.create
              - leaves.replace-month
              - attendance.clock-in
              - attendance.clock-out
              - attendance.approve-hours
              - schedule-leave-requests.submit
              - schedule-leave-requests.cancel
              - leave-requests.submit
              - leave-requests.cancel
              - time-off-requests.approve
              - time-off-requests.reject
        - in: header
          name: Idempotency-Key
          required: true
          schema: { type: string, minLength: 8, maxLength: 128 }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, additionalProperties: false }
      responses:
        '200': { description: Idempotent replay }
        '201': { description: Command committed }
        '400': { $ref: '#/components/responses/Error' }
        '401': { $ref: '#/components/responses/Error' }
        '403': { $ref: '#/components/responses/Error' }
        '409': { $ref: '#/components/responses/Error' }
        '413': { $ref: '#/components/responses/Error' }
components:
  parameters:
    WorkspaceId:
      in: header
      name: X-Workspace-Id
      required: true
      description: Untrusted requested workspace; authorization is resolved server-side from live membership.
      schema: { type: string, pattern: '^ws_[a-f0-9]{32}$' }
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: Auth0 OIDC access token (RS256)
  responses:
    Error:
      description: Structured API error
      content:
        application/json:
          schema:
            type: object
            required: [ok, error, code, requestId]
            properties:
              ok: { type: boolean, enum: [false] }
              error: { type: string }
              code: { type: string }
              requestId: { type: string }

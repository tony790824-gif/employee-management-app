This directory intentionally contains no functions. Netlify serves the static PWA only.
Production API requests use BANKE_PRODUCTION_POSTGRES_API_URL via the existing PostgreSQL API client.
The old adapters in netlify/functions are retained for reference/tests, not deployed.
